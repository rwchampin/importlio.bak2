"use client"
import * as SS from '@radix-ui/react-separator';
import styles from  "./seperator.css"

const Seperator = ({ size }) => {

    return (
      <SS className={styles.SeparatorRoot}  />

    )
}

export default Seperator